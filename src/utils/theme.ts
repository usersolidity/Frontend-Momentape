import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#833CDE',
    },
    secondary: {
      main: '#fff',
    },
  },
});

export default theme;
