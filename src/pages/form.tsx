import { Base } from "../templates/Base";
import { Form } from "../components";
const FormPage = () => {
    return (
        <Base>
            <Form />
        </Base>
    );
};

export default FormPage;
